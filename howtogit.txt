source .venv/Scripts/activate

$ git commit -a -m "JP 2020-03-04"
$ git push origin master

#instalacja bilbioteki requests (potrzebnej do uzyskania kodu html z url)
pip install requests

#instalacja beautiful soup (potrzebnej do interpretacji uzyskanego html)
pip install bs4 